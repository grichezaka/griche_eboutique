framework:
  secret: '%env(APP_SECRET)%'
  session:
    handler_id: null
    cookie_secure: auto
    cookie_samesite: lax
  csrf_protection: true
  http_method_override: false

when@prod:
  framework:
    session:
      cookie_secure: true

